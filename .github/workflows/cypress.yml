name: Cypress

on:
  workflow_dispatch:
  pull_request:

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install modules
        run: npm install
      - name: Cypress run
        uses: cypress-io/github-action@v6
